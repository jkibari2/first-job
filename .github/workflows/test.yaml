name: test runner
on: 
 push:
   branches:
     - main  
jobs:
  build:

    runs-on: [self-hosted]
    test_runner:
    steps:
      - uses: actions/checkout@v2
      - name: env
        run: env
        working-directory: /home/jkibari3/first-job


